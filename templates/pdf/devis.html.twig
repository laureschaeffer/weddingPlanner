<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&display=swap" rel="stylesheet">
    
    <title>Devis</title>
</head>
<body>
    <style>
      body{
        font-family: "Cormorant Garamond";
        font-size: 18px;
      }
        h1{
            text-align: center;
        }
        table {
            width: 100%;
          }
          
          td, th {
            text-align: left;
            padding: 8px 0;
          }
          .th-right{
            text-align: right;
          }
          img{
            text-align: center;
          }
    </style>
    <h1>Devis pour projet de mariage</h1>

    <table>
        <tr>
          <th>Entreprise</th>
          <th class="th-right">Client</th>
        </tr>
        <tr>
          <td>Cérémonie Couture</td>
          <td class="th-right">{{project.firstname}} {{project.surname}}</td>
        </tr>
        <tr>
          <td>1 rue des dev 67100 Strasbourg</td>
          <td class="th-right">{{project.telephone}}</td>
        </tr>
        <tr>
          <td>ceremonie-couture@exemple.fr</td>
          {% if project.email %}
            <td class="th-right">{{project.email}}</td>
            {% else %}
            <td class="th-right">{{project.user.email}}</td>
          {% endif %}
          <td class="th-right">{{project.email}}</td>
        </tr>
      </table>
      <br><hr>

    <p><b>Date de l'évènement: {{project.dateEvent | date("d.m.Y")}} </b></p>
    <p>Nombre d'invités: {{project.nbGuest}} </p>
    <p>Destination: {{project.destination}} </p>

    <h3>Description du projet: </h3>
    <p>{{project.description}}</p>

    <h3>Prestations choisies:</h3>
    {% for prestation in project.prestations %}
        <p>{{prestation}}</p>
    {% endfor %}

    {% if project.finalPrice %}
      <p><b>Prix fixé: {{project.finalPrice |number_format(0, ',', ' ') }} € </b></p>
    {% endif %}
    
    <h3>Commentaires</h3>
    {% for comment in project.comments %}
        <p>{{comment}}</p>
        {% else %}
        <p>Aucun commentaire ajouté</p>
    {% endfor %}



    <hr>
    <img src="data:image/png;base64,{{ imageData }}" width="250px" height="250px" alt="ceremonie couture logo">


</body>
</html>